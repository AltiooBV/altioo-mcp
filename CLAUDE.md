# CLAUDE.md

Claude Code loads this file automatically. It holds no instructions of its own: the repository
brief is [AI.md](AI.md), which every assistant reads, and it is imported below so it arrives
here too.

**If the import did not resolve, read `AI.md` now, before the first edit** — followed by
`AGENTS.md` §0 and "Using this guide", which nothing loads for you and which govern
documentation-only changes as much as code.

@AI.md
