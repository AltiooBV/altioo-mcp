# CLAUDE.md

No instructions of its own. The repository brief is [AGENTS.md](AGENTS.md) — the file other
assistants load by name — and it is imported below so Claude Code receives it too.

**If the import did not resolve, read `AGENTS.md` now, before the first edit**, followed by the
development guide it points at.

@AGENTS.md
